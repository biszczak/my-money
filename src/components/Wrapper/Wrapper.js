import styled from 'styled-components'

const Wrapper = styled.div`
    width: calc(100% - 20px);
    margin-left: auto;
    margin-right: auto;
    max-width: 960px;
    padding-right: 10px;
    padding-left: 10px; 
`

export default Wrapper;